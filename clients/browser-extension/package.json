{
  "name": "hip-browser-extension",
  "private": true,
  "version": "0.1.27",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test --config playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
