# V1 staging load evidence — 2026-08-01

## Scope and provenance

- Deployed application revision: `e00b47312bfa331eb788a6768019b286c4a17e43`
- Load harness revision: `9a9178e2242a8170a898f06398df2b0061917eaa`
- Target: Hostinger private staging through `https://admin.guardwithhip.com`
- Host shape: 4 vCPU, 15,992 MiB memory
- Writes: disabled
- Seeded lookup domain: `guardwithhip.com`

The first unpaced diagnostic run used the API host and failed because that host
returned 404 for the selected public lookup while the scan endpoint correctly
enforced its 60-per-minute limiter. It generated no feedback writes. That run
exposed a harness defect: scenarios could not be isolated or paced, and status
codes were not reported. Revision `9a9178e` added process-wide pacing, scenario
selection, status-code counts, network-error counts, and regression coverage.

## Cached public lookup

- Duration: 120 seconds
- Concurrency: 10
- Requested rate: 100 requests/second
- Requests: 11,816
- Achieved rate: 98.47 requests/second
- HTTP 200: 11,816
- Errors: 0
- p95: 8.09 ms
- V1 target: under 150 ms
- Result: **pass**

Observed application saturation remained low: Web CPU samples were 0.65–4.24%
and memory remained approximately 105–106 MiB. PostgreSQL CPU samples were
0–3.94% and memory approximately 49–51 MiB. Redis CPU samples were 0.48–5.75%
and memory approximately 4.4 MiB.

RabbitMQ showed transient `docker stats` CPU samples despite no queue network
traffic or queued messages. A five-second RabbitMQ runtime-thread diagnostic
showed scheduler threads 99.94% asleep, `docker top` showed 0.3% lifetime CPU,
and the broker reported fully booted/running. The transient Docker samples were
therefore not correlated with HIP load or broker work.

## Browser fast score

- Duration: 55 seconds
- Concurrency: 4
- Requested/achieved rate: 1 request/second
- Requests: 55
- HTTP 200: 55
- Errors: 0
- p95: 21.51 ms
- V1 target: under 750 ms
- Result: **pass**

The one-request-per-second rate intentionally stays within the deployed
60-per-minute public scan limit. Web CPU samples were 0.66–1.01%, PostgreSQL
0–3.99%, Redis 0.46–3.24%, and RabbitMQ 0.19–0.59%. Memory remained stable.

## Conclusion

The two unauthenticated V1 latency scenarios pass on the target staging host
with zero request errors and ample observed headroom. Public feedback writes
remain intentionally excluded because they create durable data. The protected
admin paged-list scenario still requires an authenticated load-test principal
or session and must not use development headers as production evidence.
