#!/usr/bin/env bash
set -euo pipefail

CONFIGURATION="${1:-Release}"
echo "[placeholder] Build macOS PKG for HIP.Agent.Worker (${CONFIGURATION})."
echo "TODO: add pkgbuild/productbuild + notarization flow."
